[Unit]
Description=Reset Companion's active_low=1 on GPIO4 so polarity matches other pins
After=companion.service
Wants=companion.service

[Service]
Type=oneshot
ExecStart=/usr/local/bin/gpio-fixup.sh
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
