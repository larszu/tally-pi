# Was NICHT auf die Projektseite gehoert — ein Pfad je Zeile, dahinter `#`
# und der GRUND. Der Grund ist Pflicht; `build-site.py` bricht ohne ihn ab,
# und ebenso bei einem Eintrag, dessen Datei es nicht mehr gibt.

setup-guide.html  # Nicht als nackte Datei an den Wurzelpfad: sie holt ihren gesamten Inhalt per fetch von ihrem Host (/ipconfig, /gpio, /tally-config, /atem, /logs ...) und bliebe dort leer. Auf der Seite liegt sie seit 2026-09-12 trotzdem -- unter /demo/, zusammen mit den Antworten, die scripts/build-demo.py von den echten guide_server-Funktionen ausrechnen laesst. Dort ist sie die Anwendung; hier waere sie eine Attrappe.
